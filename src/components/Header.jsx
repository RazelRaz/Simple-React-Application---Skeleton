

const Header = ({title}) => {
    return (
        <div style={{background:'#ccc', padding:'10px 0'}}>
            <h1 style={{textAlign:'center'}}>{title}</h1>
        </div>
    );
};

export default Header;